<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                    mc:Ignorable="d">

    <ResourceDictionary.MergedDictionaries>
        <materialDesign:BundledTheme BaseTheme="Light" PrimaryColor="Grey" SecondaryColor="Teal" />
        <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
        <ResourceDictionary Source="..\Colors.xaml" />
        <ResourceDictionary Source="..\Fonts.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="SWindow" TargetType="Window">
        <Setter Property="Window.ResizeMode" Value="NoResize" />
        <Setter Property="Window.WindowStyle" Value="None" />
        <Setter Property="Window.Background" Value="{x:Null}" />
        <Setter Property="Window.AllowsTransparency" Value="True" />
        <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        <Setter Property="SnapsToDevicePixels" Value="True" />

        <Setter Property="Window.Title" Value="BIM Leaders" />
        <Setter Property="Window.Width" Value="300" />
        <Setter Property="Window.Height" Value="400" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Window">
                    <Grid Background="{x:Null}">
                        <d:Card Margin="10" UniformCornerRadius="30" Background="{x:Null}">
                            <StackPanel>
                                <StackPanel.Background>
                                    <LinearGradientBrush StartPoint="0.1,0" EndPoint="0.3,1">
                                        <GradientStop Color="{Binding Source={StaticResource SColorBackgroundMain1}, Path=Color}" Offset="1"/>
                                        <GradientStop Color="{Binding Source={StaticResource SColorBackgroundMain2}, Path=Color}" Offset="0"/>
                                    </LinearGradientBrush>
                                </StackPanel.Background>

                                <AdornerDecorator>
                                    <ContentPresenter />
                                </AdornerDecorator>

                            </StackPanel>
                        </d:Card>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>

</ResourceDictionary>